import { FormatPropertyNamePipe } from './format-prop.pipe';

describe('FormatProp', () => {
  it('create an instance', () => {
    const pipe = new FormatPropertyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
